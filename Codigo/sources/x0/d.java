package x0;

import ck.v;
import gk.Continuation;

/* loaded from: classes.dex */
public interface d<T> {
    Object a(T t10, Continuation<? super Boolean> continuation);

    Object b(T t10, Continuation<? super T> continuation);

    Object c(Continuation<? super v> continuation);
}
